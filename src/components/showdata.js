import React from "react";

const ShowData = () => {
  return (
    <div>
      {/* <ul>
        {dates.map((date) => date.forEach(<li>date.name</li>))}
      </ul> */}
    </div>
  );
};
export default ShowData;
